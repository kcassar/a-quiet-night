{
  "name": "sleep-apnea-companion",
  "version": "0.1.0",
  "private": true,
  "description": "Free educational site about sleep apnoea with CPAP data upload and dashboard.",
  "scripts": {
    "install:all": "npm install && npm install --prefix server && npm install --prefix client",
    "dev": "NODE_ENV=development sh -c 'cd server && npm run dev' & NODE_ENV=development sh -c 'cd client && npm run dev' & wait",
    "build": "npm run build --prefix client && npm run build --prefix server",
    "start": "node server/dist/index.js",
    "postinstall": "npm install --prefix server && npm install --prefix client",
    "notify": "node scripts/notify-search-engines.mjs"
  },
  "dependencies": {
    "@vitejs/plugin-react": "^6.0.2",
    "react-refresh": "^0.18.0",
    "tsx": "^4.22.4",
    "vite": "^8.0.16"
  }
}
